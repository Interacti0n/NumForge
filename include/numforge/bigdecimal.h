#ifndef NUMFORGE_BIGDECIMAL_H
#define NUMFORGE_BIGDECIMAL_H

#include <stddef.h>
#include <stdbool.h>

/*
 * BigDecimal is intentionally opaque. Its public operations are not exposed
 * until the initial implementation is complete; see docs/BIGDECIMAL_DESIGN.md
 * for the representation and planned API.
 */
typedef struct BigDecimal BigDecimal;

#endif
